# Domain docs

This is a single-context repository. Read CONTEXT.md and relevant decisions under docs/adr/ before domain work, when those files exist. If absent, proceed silently; do not create placeholders. Domain-modeling work can create them when actual terms or decisions are settled.

Use the glossary's vocabulary when naming concepts. Surface any conflict with an existing architecture decision rather than silently replacing it.
